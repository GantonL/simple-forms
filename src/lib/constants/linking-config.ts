/**
 * Configuration for fields that can be linked to other fields.
 * A field type included in this list effectively enables the UI
 * for selecting a source field of the same type to link from.
 */
export const LINKABLE_FIELD_TYPES = ['date'];
